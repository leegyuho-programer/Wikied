import { ReactComponent as ArticleStrokeSVGIcon } from './ArticleStrokeIcon.svg';
import styles from './StrokeIcon.module.css';

export default function ArticleStrokeIcon() {
  return <ArticleStrokeSVGIcon className={styles.container} />;
}
